# CLAUDE.md

The project's working agreement is in **[AGENTS.md](AGENTS.md)**. Read it
before changing anything, along with the two documents it points to:
`docs/design/ui-guidelines.md` for anything that renders, and
`docs/design/2026-09-11-7dtd-panel-design.md` for what the game's API
actually does.

Kept as a pointer rather than a copy so the two cannot drift apart.
